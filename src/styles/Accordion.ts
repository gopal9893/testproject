import { colors } from "./theme";

export const style = {
  accordionDetails: {
    borderTop: "0.1rem solid" + colors.secondaryLight,
  },
  accordionSDiv: {
    display: "grid",
    gridTemplateColumns: "89% auto",
  },
};
